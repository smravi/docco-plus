<!DOCTYPE html>
<html>
<head>
    <!-- FIXME: title -->
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <meta name="viewport"
        content="width=device-width, target-densitydpi=160dpi, initial-scale=1.0; maximum-scale=1.0; user-scalable=0;">
    <% for( var i = 0, j = options.resources.css.length; i < j; i++) { %>
    <link rel="stylesheet"
        media="all"
        href="<%= path.relative(path.dirname(destination), options.resources.css[i]) %>"/>
    <% } %>

    <% for( var i = 0, j = options.resources.js.length; i < j; i++) { %>
    <script src="<%=path.relative(path.dirname(destination), options.resources.js[i])  %>"
        type="text/javascript"></script>
    <% } %>

    <script type="application/json" id="jsTreeJSON">
        <%= JSON.stringify(fileArray) %>
    </script>
</head>
<body>
<h1>
    <%=baseName %>
</h1>
<div class="subHeading">
    <%=srcPath %>
</div>
<button type="button" class="navigatorToggle">&#9776; files...</button>
<nav class="navigationTree minimized">
</nav>
<section>
    <ul class="sectionDetails <%=sectionUlClass %>">
        <% for (var i=0, j=sections.length; i < j; i++) { %>
        <% var section = sections[i]; %>
        <li id="section-<%= i + 1 %>">
            <div class="annotation">
                <%= section.docsHtml %>
            </div><div class="content">
                <%= section.codeHtml %>
            </div>
        </li>
        <% } %>
    </ul>
</section>
</body>
</html>
